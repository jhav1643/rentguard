backend-api has been deployed to namespace {{ .Release.Namespace }}.

Check rollout status:
  kubectl rollout status deployment/{{ include "chart.fullname" . }} -n {{ .Release.Namespace }}

Access it locally (no LoadBalancer, no billed ELB):
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "chart.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }}

Then open:
  http://localhost:{{ .Values.service.port }}/health

If the pod is stuck in ImagePullBackOff:
  kubectl describe pod -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "chart.name" . }}

The env Secret ({{ .Values.envSecretName }}) must exist BEFORE install:
  kubectl create secret generic {{ .Values.envSecretName }} \
    --namespace {{ .Release.Namespace }} --from-env-file=.env